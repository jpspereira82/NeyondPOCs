# Vybe POC — variáveis de ambiente
# A POC frontend funciona standalone com mock data layer em localStorage.
# Estas variáveis são apenas para o backend Spring Boot (futuro).

DATABASE_URL=postgresql://neondb_owner:npg_WZyIrj1XwxL3@ep-lucky-art-apxx5md5.c-7.us-east-1.aws.neon.tech:5432/neondb?sslmode=require

# JWT
JWT_PRIVATE_KEY_PATH=./keys/jwt-private.pem
JWT_PUBLIC_KEY_PATH=./keys/jwt-public.pem
JWT_ACCESS_TTL_SECONDS=3600
JWT_REFRESH_TTL_SECONDS=604800

# Azure AI Language (sentiment)
AZURE_LANGUAGE_ENDPOINT=
AZURE_LANGUAGE_KEY=

# Azure Communication Services (email)
AZURE_ACS_CONNECTION_STRING=
ACS_SENDER_ADDRESS=no-reply@vybe.neyond.pt

# Frontend (Vite)
VITE_API_BASE_URL=http://localhost:8080/api/v1